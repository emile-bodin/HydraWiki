You are writing a repository-specific, source-derived wiki for a developer who is new to the indexed repository. The internal title is `__TITLE__`.

Return JSON only with exactly these two keys: `structure` and `pages`; each page has exactly: content (non-empty string) and citations (non-empty array). Never return fabricated content.

When useful, organize page content around these ordinary concepts: `## System context`, `## Architecture overview`, `## Main components`, `## Key workflows`, `## Constraints and failure behavior`, and `## Quick reference`; the first five are the exactly these five top-level sections of the reader contract. Use `### Purpose`, `### Overview`, and `### Installation` only when supported. Do not use inline CSS.

The `structure` must be an array containing exactly these five groups, in this order. Keep every group even when it has no supported pages:

[
  {"key":"get-started","title":"Get started","pages":[{"path":"get-started/example","title":"..."}]},
  {"key":"concepts","title":"Concepts","pages":[]},
  {"key":"guides","title":"Guides","pages":[]},
  {"key":"reference","title":"Reference","pages":[]},
  {"key":"workflows","title":"Workflows","pages":[]}
]

Derive the complete structure from the indexed excerpts before writing page content. Create a variable number of pages: add a page only when the sources support a useful topic, and leave unsupported groups empty. Do not fabricate standard pages such as API reference, Configuration, or Operations. Every structure page must appear exactly once in `pages`, with the same `path`, `title`, and group key. Use a short lowercase slug after the fixed group prefix; never invent a page merely to fill a group.

Each `pages` item must contain exactly these keys:

{"group":"concepts","path":"concepts/example","title":"Example","content":"# Example\n...","citations":[{"path":"src/example.py","line_start":1,"line_end":2}]}

Every page content must be non-empty Markdown and every page must have at least one citation. Use only the indexed source excerpts below. Every claim must be supported by at least one citation; every citation must use the provided paths and line ranges exactly as they appear in the excerpts. If a topic is not supported, omit it.

Preserve reader-friendly Markdown, source attribution, and concise architecture-first explanations. Include Mermaid only when the excerpts support the relationship. Use standard Mermaid syntax and stable simple node IDs that contain no labels or paths. Put readable labels separately and quote labels containing spaces, paths, colons, parentheses, or punctuation, for example:

```mermaid
flowchart LR
HostRepos["/repositories (read-only)"] --> Api["API service :8000"]
Api --> Worker["Worker"]
Worker --> Database["Database"]
```

Never put a path, label, colon, or punctuation into a node ID. Do not draw relationships that are not supported by the excerpts. Do not use Mermaid directives, frontmatter configuration, classDef, class, style, linkStyle, or click syntax; the server validates diagrams fail-closed.

The indexed source excerpts follow.

__SOURCE_EXCERPTS__
